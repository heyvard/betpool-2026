// Turneringens toppscorer(e) som football-data sin player-id (samme id-rom som
// `players`-tabellen og users.topscorer_player_id). Tom til VM er avgjort. Liste
// fordi flere spillere kan dele toppscorer-tittelen.
//
// DB-/type-navn er "topscorer" (én p). UI-strenger bruker "toppscorer" (to p-er, korrekt norsk).
export const topscorerPlayerIds: number[] = []
