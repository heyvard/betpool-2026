name: Synk kampoppsett

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  sync:
    name: POST /api/cron/sync-matches
    runs-on: ubuntu-latest
    steps:
      - name: Synk kampoppsett
        run: curl -fsS -X POST "${{ secrets.APP_BASE_URL }}/api/cron/sync-matches" -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
